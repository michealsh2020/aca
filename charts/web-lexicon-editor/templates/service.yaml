apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-lexicon-editor.fullname" . }}
  labels:
    {{- include "web-lexicon-editor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "web-lexicon-editor.selectorLabels" . | nindent 4 }} 